# Google Maps JavaScript API key (required for the GIS page's Google Map view).
# Get one from https://console.cloud.google.com/google/maps-apis and restrict it to your domain.
# Without it, the GIS page falls back to the built-in SVG choropleth map.
VITE_GOOGLE_MAPS_API_KEY=
